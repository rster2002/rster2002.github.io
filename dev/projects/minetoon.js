version("a.0.5", "snapshot", ["+ Added splatzones","~ Fixed some bugs"]);
version("a.0.4", "snapshot", ["* Scores are now shown as precentages","~ Bug with splattershot when shooting over an edge fixed"]);
version("a.0.3", "snapshot", ["* fixed some bugs"]);
version("a.0.2", "snapshot", ["* bug fixes", "+ added some bugs", "~ known bug where purple team splattershot doesn't work","~ known bug that makes every map act strange exept urchin underpass"]);
version("a.0.1", "snapshot", ["* first version"]);
