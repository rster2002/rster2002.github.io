function compleetSetup() {
	sendData("test", "test");
}
/*
	if (reciveData("setupCompleet") === "true") {
		storeData("username", reciveData("username"));
		storeData("password", reciveData("password"));
		var username = loadData("username");
		document.getElementById("ext_output").innerHTML = username;
	} else {
		document.getElementById("ext_output").innerHTML = "Cant find setup info"
	}
*/